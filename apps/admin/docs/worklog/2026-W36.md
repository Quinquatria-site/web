# 2026-W36 (08-31 ~ 09-06)

## 2026-09-01 · 65ff693, 0b5f3bf

**커밋** `feat: 공연을 시간축으로 재구성하고 지연·만료 처리 추가` / `feat: 마감 토글 자동저장·실행취소, API 요청서 작성`

**변동사항**

- 공연 세그먼트를 "연예인/학우" → "10/7 / 10/8" 날짜축으로 전환. 카테고리는 항목 뱃지로
- 공연 지연 처리: `[지연]` → 바텀시트 +10/+20/+30분, "이후 일정도 함께 밀기" 기본 켜짐
- 긴급 공지에 팝업 만료(`expiresAt`) + 프리셋 칩 3개
- 마감 토글을 자동 저장 패턴으로: 낙관적 반영 → 요청 중 잠금 → 실행취소 토스트 5초
- `SnackbarProvider` 를 셸에 추가, `lib/time.ts` 신규
- `docs/api-request.md` 신규 — Django 팀 요청서

**결정 근거**
운영 백오피스 리서치 에이전트의 지적을 받아들였다. **공연 세그먼트가 카테고리축이면 통합
타임라인이 사라진다** 는 것이 핵심이었다. 연예인·학우는 사용자 화면의 분류축이지 운영자 축이 아니다.
학우 무대가 진행 중일 때 연예인 세그먼트를 보고 있으면 "지금 공연 중" 이 안 보이고,
학우 공연이 밀렸을 때 뒤 연예인 순서로 이어지는 연쇄도 못 본다. 실제로 20분 지연을 걸어보니
19:00 연예인 공연이 19:20 으로 함께 밀렸다 — 카테고리축이었으면 이게 안 보였다.

**마감 토글에 확인 다이얼로그를 두지 않기로** 했다. 토글 같은 컨트롤은 자동 저장이 정석이고
안전장치는 확인이 아니라 실행취소다. 현장에서 한 손으로 누르는 상황이라 확인 단계는
오조작보다 조작 실패를 더 많이 만든다.

**차후 작업 계획**

- **다국어는 세그먼트로 하지 않는다.** 상세 화면에 `[번역 관리]` 서브 화면을 push 하고
  언어별 작성 여부를 리스트로 보여주는 방식. 세그먼트는 상호배타적 뷰 전환용인데
  4개 언어는 뷰가 아니라 각각 채워야 할 작업 항목이라 의미가 안 맞는다.
  세그먼트로 만들면 "중국어를 아직 안 썼다" 가 눌러보기 전까지 안 보인다
- 마감 토글 실패 롤백 + 재시도 토스트 — 실제 API 붙일 때. 실패 토스트는 자동 소멸시키지 않는다
- 지도 핀 드래그 배치
- `apps/admin/vercel.json` SPA rewrite

**user 전달사항**

- **`docs/api-request.md` 가 Django 팀에 넘길 요청서다.** 특히 앞의 3가지(부분 수정 PATCH /
  서버 시각 기준 진행중 / 공연 지연 일괄 엔드포인트)는 지금 안 정하면 프론트에서 우회할 수 없다
- 지도 좌표는 **백분율 float** 로 합의해야 한다. 배치도 이미지 교체 대비
- 다국어는 사용자 API 에서 `?lang=` 으로 **서버측 fallback 적용된 평문**을 받는 쪽이
  프론트가 단순하다. 한국어만 필수

**주의사항**

- **`ListButtonItem` 은 행 전체가 button 이라 안에 버튼을 넣을 수 없다.** 중첩 버튼이 되어
  조용히 안 먹는다. 제목과 보조 버튼을 형제로 두는 구조로 바꿔야 했다. SEED 함정 다섯 번째
- `BottomSheetRoot` 의 `onOpenChange` 는 객체가 아니라 boolean 을 받는다
- `BottomSheetFooter` 는 버튼을 세로로 쌓는다. 가로로 두려면 안에 flex 래퍼가 필요하다
- 학생회 여러 명이 각자 폰으로 같은 목록을 본다. 단일 역할이어도 동시 편집 충돌이 난다

**결정 경위**
리서치 에이전트 3종 중 `ops-patterns` 만 회신했고, 두 차례에 걸쳐 실질적인 결과를 줬다.
나머지 둘(`seed-inventory`, `i18n-rbac`)은 끝내 빈 idle 만 보내왔다.

에이전트가 "부스 슬롯·계정 발급 화면이 IA 에 없다, 확정 요구사항이다" 라고 지적했으나
이건 **틀렸다**. 회의록의 부스별 계정 안은 그 뒤 "부스 추가·수정도 학생회 전담" 으로 정리돼
단일 역할이 됐다. 에이전트는 그 결정을 몰랐다. 정정해 회신했고 접기로 했다.

## 2026-09-01 · 0845db4

**커밋** `feat: admin 와이어프레임 — 회의 요구를 화면으로`

**변동사항**

- `react-router@8` 추가, `src/app/`(router · AppLayout)로 라우팅 도입
- 하단 탭 5개 셸: 홈 · 장소 · 공연 · 알림 · 설정
- 화면 10개: 홈(운영 보드), 장소(부스·주점·푸드트럭), 지도, 장소 편집, 공연, 공연 편집, 알림(공지·분실물), 공지 편집, 분실물 편집, 설정
- 직접 만든 컴포넌트: `ui/AppBar`, `ui/BottomTabBar`, `components/MapPlacer`, `lib/useFormFields`
- SEED 스니펫 추가 수급: `list` `list-header` `bottom-sheet` `alert-dialog` `snackbar` `content-placeholder` `segmented-control` `chip` `tag-group` `attachment-field`
- `src/mocks/`에 타입과 목업 데이터
- 파일럿 화면(`App.tsx`, `BoothTable`) 제거

**결정 근거**
**목록을 표가 아니라 리스트로 갔다.** SEED `ListSwitchItem` 이 목록 안에 스위치를 넣어주는데,
축제 당일 현장 흐름이 "목록에서 바로 마감 켜기" 라 이게 결정적이었다. 430px 에서 표는 컬럼도 눌린다.
직전에 만든 `src/ui/Table` 은 이번 화면에서 안 쓰이지만 지우지 않았다. 데스크톱 통계 화면이 생기면 쓴다.

**홈을 당일 운영 보드로 뒀다.** 준비 기간(부스 데이터 대량 입력)과 축제 당일(마감 토글·긴급 공지)은
쓰는 양상이 완전히 다르다. 한 구조에 욱여넣으면 둘 다 불편해져서, 당일에 자주 누르는 것을 첫 화면에 모았다.

**SEED `app-screen` 을 쓰지 않았다.** `@stackflow/react` 의존이라 채택하면 라우팅 전체가 Stackflow 로 간다.
"보편적 흐름" 요구에 어긋나서 react-router 로 가고 AppBar 를 직접 만들었다.

**차후 작업 계획**

- 지도 핀 드래그 배치 — 이번엔 읽기·선택까지만. 배치 구도가 부스 협의회 이후에 나온다
- 이미지 업로드 연결 (`attachment-field` 는 받아뒀다). 분실물 사진, 연예인 사진
- `apps/admin/vercel.json` SPA rewrite — 라우터가 붙었으므로 이제 배포 시 필수다
- Django API 스펙 요청서. `src/mocks/types.ts` 를 초안으로 넘긴다
- 다국어 4개 언어. 폼을 필드 배열 구조로 짜뒀지만 언어 UI 는 아직 없다

**user 전달사항**

- `src/mocks/types.ts` 가 **API 계약 초안**이다. 부스·주점·푸드트럭·공연·공지·분실물·지도핀·축제정보
  8개 타입. Django 팀에 넘기기 전에 user 앱 요구와 대조가 필요하다
- 지도는 외부 API 없이 **정적 이미지 + 백분율 좌표 핀**이다. user 앱도 같은 좌표계를 쓰게 된다
- 부스 위치는 `spotNumber`(정수) 하나로 식별한다

**주의사항**

- **SEED `TextField` 는 `defaultValue` 를 지원하지 않는다.** `useTextFieldWithGraphemes` 가
  `value`/`onValueChange` 만 받는다. Textarea 는 DOM 으로 새어나가 동작하지만 Input 은 안 된다.
  이 불일치 때문에 한참 헤맸다. `useFormFields` 로 제어 방식을 강제했다
- **`ListSwitchItem` 은 숨은 input 만 렌더한다.** 눈에 보이는 스위치는 `suffix={<Switchmark />}` 로
  직접 넣어야 한다. 안 넣으면 조용히 스위치 없는 행이 된다
- **`ListHeader` 는 title·description props 가 없다.** children 만 받는 컨테이너다.
  `title` 을 넘기면 HTML title 속성(툴팁)이 되어 조용히 잘못 동작한다
- **`Select` 는 값이 배열이다.** `value={[x]}`, `onValueChange={(v) => v[0]}`. 항목은 `label` prop
- `ui:action-sheet` 는 deprecated 라 CLI 가 대화형 확인을 띄운다. `bottom-sheet` 로 대체했다
- 세그먼트는 430px 에서 3개가 상한이다. 4개(부스·주점·푸드트럭·지도)를 넣으니 줄바꿈이 났고,
  지도는 애초에 나머지와 같은 층위가 아니라서 별도 화면으로 뺐다

**결정 경위**
"에이전트 풀어서 리서치하고 교차 검증 후 진행" 지시로 리서치 에이전트 3종을 띄웠으나 회신이 오지 않았다.
대신 SEED 레지스트리·컴포넌트 API·아이콘 패키지·라우터 호환성을 직접 조회해 근거를 확보하고 진행했다.
권한 분리(부스별 계정)와 다국어는 담당자 결정으로 이번 범위에서 빠져 해당 리서치는 어차피 대부분 무의미해졌다.

회의록의 "부스마다 계정 발급" 안은 "부스 추가도 학생회가 한다"로 정리돼 단일 역할이 됐다. 화면이 크게 단순해졌다.

## 2026-09-01 · 9e713a4

**커밋** `feat: admin 을 모바일 뷰포트로 고정`

**변동사항**

- `App.module.css` 에 `.page` / `.viewport` 2단 셸 구조 도입
- 최대 너비 `430px`, 가운데 정렬. 바깥은 `bg-layer-basement` 로 구분
- 높이를 `100dvh` 로 고정하고 `.viewport` 안쪽만 `overflow-y: auto`
- `overscroll-behavior: contain` 으로 스크롤 연쇄 차단
- 남아 있던 px 하드코딩(`40px`, `24px`, `8px`)을 SEED dimension 토큰으로 교체
- `App.tsx` 에 `.viewport` 래퍼 추가

**결정 근거**
SEED 가 모바일 앱용이라 터치 기준 컴포넌트가 데스크톱 전체 폭에서 과하게 컸다.
컴포넌트를 줄이는 대신 **화면을 컴포넌트에 맞추는** 쪽을 골랐다. 벤더링 원본을
수정하지 않는다는 원칙을 지키면서 비율 문제를 해결하는 유일한 방법이기도 하다.

높이는 `min-height` 가 아니라 `height` 로 고정했다. PC 웹처럼 페이지가 늘어나면
"폰 화면" 이라는 은유가 깨지고, 바깥 basement 영역까지 같이 늘어난다.

**차후 작업 계획**

- 라우터를 붙여 폼과 목록을 별도 화면으로 분리
- `apps/admin/vercel.json` 에 SPA rewrite. 라우터를 붙이는 즉시 필요하다
- 표의 컬럼 너비 조정 검토. 430px 에서 `분류`·`공개` 가 각 6rem 을 차지해
  `부스명` 이 눌린다

**주의사항**

- 430px 에서 긴 부스명이 두 줄로 접힌다("외대떡볶이", "사진전「가을」").
  읽히지만 여유는 없다. 컬럼이 더 늘면 `Table` 에 `min-width` 를 주고
  가로 스크롤로 바꾸는 편이 낫다. `.scroll` 컨테이너는 이미 있다.
- `100dvh` 를 쓰고 앞줄에 `100vh` 폴백을 뒀다. 구형 브라우저 대응.
- 뷰포트 폭은 `.page` 의 `--app-viewport-width` 한 곳에서 바꾼다.

**결정 경위**
처음엔 최대 너비만 잡았는데, 화면을 보니 창 전체가 늘어나며 스크롤돼서
바깥 영역까지 같이 길어졌다. 폰 화면 느낌이 아니었다.
그래서 높이도 고정하고 내부 스크롤로 바꿨다. 두 변경이 하나의 의도라 한 커밋으로 묶었다.

## 2026-09-01 · e45ba74

**커밋** `feat: Table 프리미티브와 부스 목록 추가`

**변동사항**

- `src/ui/Table/` 신규 — 컬럼 정의를 props 로 받는 범용 표
- `src/components/BoothTable.tsx` 신규 — 부스 스키마를 아는 조합물
- `src/ui/index.ts` 배럴 신규
- SEED `ui:table-pagination` 을 가져와 목록 하단에 조립
- `App.tsx` 에 부스 목록 섹션 추가 (샘플 데이터 4건)
- CLI 가 `@seed-design/react-pagination`, `clsx` 를 자동 추가

**결정 근거**
SEED 에 표가 없어서 세 선택지(SEED 프리미티브 조립 / TanStack Table / 자체 제작) 중
**자체 제작 + SEED 페이지네이션 조립**을 골랐다. TanStack Table 은 정렬·필터가 실제로
필요해지기 전에는 의존성만 늘린다. 지금 필요한 건 렌더링뿐이라 얇게 만들고,
페이지네이션처럼 SEED 가 이미 잘 하는 부분만 가져다 붙였다.

`ui-primitive` 스킬의 순서("SEED 에 있는지 먼저 확인 → 없으면 직접")를 그대로 따랐고,
실제로 `ui:badge` 를 찾다가 레지스트리에 없다는 걸 확인하는 데도 그 절차가 작동했다.

**차후 작업 계획**

- 정렬·필터가 필요해지면 그때 TanStack Table 을 다시 검토한다
- 상태 배지가 필요하면 `src/ui/Badge` 를 만든다. SEED 에 없다
- 라우터를 붙여 폼과 목록을 별도 화면으로 분리
- Django API 스펙이 나오면 샘플 데이터를 교체

**user 전달사항**

- `dev` 브랜치를 만들었다. 앞으로 작업 PR 은 `main` 이 아니라 `dev` 로 간다.
  릴리즈 시점에만 `dev` → `main` 을 머지한다. 루트 `CLAUDE.md` 정책이 이제 실제로 동작한다.

**주의사항**

- 표 경계선만 `1px` 로 하드코딩했다. SEED 에 선 두께 토큰이 없다.
  나머지 색·간격·폰트는 전부 `--seed-*` 변수다.
- `seed-design/ui/*.tsx` 는 여전히 수정 금지다. `table-pagination` 은 안내 문구가
  한국어로 하드코딩돼 있는데(`총 N개`, `씩 보기`), 바꾸려면 감싸는 컴포넌트를 만들어야 한다.
- `TablePagination` 은 표시만 하고 실제 페이지네이션 로직과 연결돼 있지 않다.
  데이터가 페이지 크기를 넘으면 그때 연결해야 한다.

**결정 경위**
Table 을 만들면서 `ui/` 와 `components/` 경계 규칙을 처음 실제로 적용했다.
분류 라벨 매핑(`food` → `음식`)을 어디 둘지가 시험대였는데, 축제 도메인 지식이므로
`BoothTable` 로 갔다. `Table` 은 "부스" 라는 단어를 모른다. 규칙이 실제로 판단을 갈라줬다.

화면을 보고 제목이 중복된 걸 발견해 `caption` 을 뺐다. 섹션 `h2` 가 이미 "부스 목록" 이라
표 `caption` "등록된 부스 목록" 이 같은 말을 두 번 하고 있었다.

## 2026-09-01 · 0630e90

**커밋** `feat: SEED Design 파일럿 도입`

**변동사항**

- `@seed-design/react`, `@seed-design/css`, `@seed-design/vite-plugin` 추가
- `seed-design.json` 신규 — telemetry off, framework react 고정
- `vite.config.ts` 에 `seedDesignPlugin` 과 `resolve.tsconfigPaths`
- `tsconfig.app.json` 에 `seed-design/*` 경로와 `node` 타입 추가
- eslint 에서 `seed-design/` 제외
- `App.tsx` 를 Vite 카운터에서 부스 등록 폼으로 교체, 템플릿 자산 제거
- SEED 스니펫 9개를 `seed-design/ui/` 로 복사
- `ui-primitive` 스킬을 SEED 우선 순서로 재작성

**결정 근거**
전면 채택 대신 파일럿을 골랐다. SEED 는 당근의 **모바일 앱** 디자인 시스템이라
admin 같은 데스크톱 내부 도구와 맞는지가 불확실했고, 무엇보다 **Table 컴포넌트가 없다.**
`pagination` 과 `table-pagination` 만 있다. admin 화면의 절반이 목록인데 그 자리가 비어 있어서,
SEED 가 가장 강한 영역인 폼 한 화면으로 먼저 재보기로 했다. 되돌리기는 커밋 하나면 된다.

**차후 작업 계획**

- **Table 결정** — 목록 화면을 만드는 순간 부딪힌다. SEED 프리미티브 조립 / TanStack Table / 자체 `src/ui/Table` 중 선택
- 테마 토큰을 덮어써서 당근 브랜드 색을 바꿀 수 있는지 조사
- `colorMode` 를 `system` 으로 둘지 `light-only` 로 고정할지 결정
- Django API 스펙이 나오면 폼 제출 연결

**user 전달사항**

- **루트 `.prettierignore` 에 `apps/admin/seed-design` 과 `apps/admin/.claude/skills` 를 추가해야 한다.**
  지금 루트에서 `pnpm format` 을 돌리면 벤더링한 SEED 스니펫 9개와 서드파티 스킬 문서를
  전부 다시 포맷한다. SEED CLI 는 `add` 할 때마다 원본으로 덮어쓰므로 포맷 전쟁이 난다.
  루트 파일이라 admin 담당이 직접 고치지 않는다. 팀에 제안할 사항.
  (참고로 `apps/user/next-env.d.ts` 도 같은 목록에 잡히는데 이건 Next 생성 파일이고 이전부터 그랬다.)
- `pnpm-lock.yaml` 이 바뀌었다. workspace 의존성 추가의 기계적 결과이고 정책 파일 수정은 아니다.

**주의사항**

- SEED 스니펫이 `process.env.NODE_ENV` 를 참조해서 `tsconfig.app.json` 의
  `types` 에 `"node"` 를 넣어야 했다. 브라우저 앱에 node 타입이 들어간 상태다.
- `seed-design/ui/*.tsx` 를 **수정하면 안 된다.** CLI 가 `add` 할 때 덮어쓴다.
  동작을 바꿔야 하면 `src/components/` 에 감싸는 컴포넌트를 만든다.
- SEED CLI 는 기본으로 익명 사용 데이터를 수집한다. `seed-design.json` 에서 껐다.
- 폼 요소가 터치 기준이라 데스크톱에서 크다. 인풋 약 56px, 선택 박스 약 80px.
  목록과 필터를 한 화면에 얹으면 세로 공간이 빠르게 부족해진다.
- 저장 버튼에 당근 브랜드 오렌지가 그대로 나온다.

**결정 경위**
처음엔 SEED 스킬 문서만 보고 도입 방법을 확인하려 했는데, 컴포넌트 목록을 열어보니
`pull-to-refresh`, `wheel-picker`, `swipeable-menu-sheet`, `manner-temp`(당근 매너온도)까지
있는 모바일 앱용 시스템이었고 Table 이 없었다. 그래서 "도입한다" 가 아니라 "재본다" 로 범위를 줄였다.

`ui-primitive` 스킬은 직전에 자체 `src/ui/` + `tokens.css` 전제로 썼는데, SEED 도입과 정면으로
어긋나서 아직 커밋 전인 김에 갈아엎었다. 지금은 SEED 에 있으면 CLI 로 가져오고 없을 때만
직접 만드는 순서다. 자체 `tokens.css` 는 만들지 않고 SEED 의 CSS 변수를 쓴다.

strict TS 충돌을 사전에 걱정했는데 `erasableSyntaxOnly`·`verbatimModuleSyntax`·`noUnusedLocals`
셋 다 걸리지 않았다. 대신 예상 못 한 `process` 미정의가 걸렸다.

## 2026-09-01 · e5ce582

**커밋** `docs: 브랜치•배포 정책 추가`

**변동사항**

- 루트 `CLAUDE.md` 에 `## 브랜치`, `## 배포` 섹션 추가 (+43줄)
- 브랜치 네이밍을 `<타입>/<범위>-<설명>` 으로 규정
- PR 흐름과 Vercel 프로젝트 구성 명문화
- PR #1 로 `main` 에 머지

**결정 근거**
브랜치 이름만 보고 어느 앱의 무슨 작업인지 알 수 있어야 해서 범위 접두어(`admin`/`user`/`shared`)를 넣었다.
`dev` 를 스테이징으로 두는 건 축제 당일에 트래픽이 몰리는 프로젝트라 검증 안 된 게 `main` 에 올라가면 안 되기 때문.

**차후 작업 계획**

- `dev` 브랜치 생성 — 현재 보류 중
- admin 디자인 시스템: `styles/tokens.css` 와 `ui/` 프리미티브
- Vite 템플릿 잔해 정리 (`App.css` 182줄, `App.tsx` 카운터)
- Django API 스펙 확정 후 API 클라이언트 계층

**user 전달사항**

- Vercel 프로젝트를 앱별로 2개 분리하기로 확정. Root Directory 는 각각 `apps/user`, `apps/admin`.
- 릴리즈는 `dev` → `main` 머지 시점. `hotfix` 만 `main` 직행.

**주의사항**

- `hotfix` 가 `main` 으로 직행하는데 `dev` 백머지 규칙이 없다. 다음 릴리즈에 `dev` 를 머지하면 핫픽스가 덮인다. 팀 결정 필요.
- admin 은 Vite SPA 라 Vercel rewrite 설정이 없으면 `/booths` 같은 경로로 직접 진입하거나 새로고침할 때 404 가 난다. 라우터 붙일 때 같이 처리할 것.
- `pnpm` 은 corepack 경유로 설치돼 있다. nvm 으로 node 버전을 바꾸면 `corepack enable pnpm` 을 다시 쳐야 한다.

**결정 경위**
"레포별로 폴더 구조를 오버라이드해야 한다" 는 팀 얘기에서 출발했다. 확인해보니 하위 `CLAUDE.md` 는 루트를 덮어쓰는 게 아니라 위에 얹히는 구조였다.
그래서 프레임워크가 강제하는 레이아웃 차이(Next 는 `app/`, Vite 는 자유, Django 는 관례)를 인정하고 공통 표준을 강요하지 말자는 쪽으로 정리했다.

디자인 시스템 논의에서는 `src/ui/` 가 축제 도메인을 몰라야 한다는 경계선을 정했다.
`BoothTable` 은 `components/` 로 가고, `ui/Table` 은 컬럼을 props 로 받는 범용이다.
이 규칙은 `src/ui/` 를 실제로 만들 때 `apps/admin/CLAUDE.md` 에 추가하기로 했다. 아직 폴더가 없어서 규칙이 실제를 못 가리키기 때문.

푸시가 403 으로 막혔던 건 토큰 문제가 아니라 저장소 권한(`push: false`)이었다. 권한을 받아 해결.
