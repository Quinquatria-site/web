# 2026-W36 (08-31 ~ 09-06)

## 2026-09-01 · 0630e90

**커밋** `feat: SEED Design 파일럿 도입`

**변동사항**

- `@seed-design/react`, `@seed-design/css`, `@seed-design/vite-plugin` 추가
- `seed-design.json` 신규 — telemetry off, framework react 고정
- `vite.config.ts` 에 `seedDesignPlugin` 과 `resolve.tsconfigPaths`
- `tsconfig.app.json` 에 `seed-design/*` 경로와 `node` 타입 추가
- eslint 에서 `seed-design/` 제외
- `App.tsx` 를 Vite 카운터에서 부스 등록 폼으로 교체, 템플릿 자산 제거
- SEED 스니펫 9개를 `seed-design/ui/` 로 복사
- `ui-primitive` 스킬을 SEED 우선 순서로 재작성

**결정 근거**
전면 채택 대신 파일럿을 골랐다. SEED 는 당근의 **모바일 앱** 디자인 시스템이라
admin 같은 데스크톱 내부 도구와 맞는지가 불확실했고, 무엇보다 **Table 컴포넌트가 없다.**
`pagination` 과 `table-pagination` 만 있다. admin 화면의 절반이 목록인데 그 자리가 비어 있어서,
SEED 가 가장 강한 영역인 폼 한 화면으로 먼저 재보기로 했다. 되돌리기는 커밋 하나면 된다.

**차후 작업 계획**

- **Table 결정** — 목록 화면을 만드는 순간 부딪힌다. SEED 프리미티브 조립 / TanStack Table / 자체 `src/ui/Table` 중 선택
- 테마 토큰을 덮어써서 당근 브랜드 색을 바꿀 수 있는지 조사
- `colorMode` 를 `system` 으로 둘지 `light-only` 로 고정할지 결정
- Django API 스펙이 나오면 폼 제출 연결

**user 전달사항**

- **루트 `.prettierignore` 에 `apps/admin/seed-design` 과 `apps/admin/.claude/skills` 를 추가해야 한다.**
  지금 루트에서 `pnpm format` 을 돌리면 벤더링한 SEED 스니펫 9개와 서드파티 스킬 문서를
  전부 다시 포맷한다. SEED CLI 는 `add` 할 때마다 원본으로 덮어쓰므로 포맷 전쟁이 난다.
  루트 파일이라 admin 담당이 직접 고치지 않는다. 팀에 제안할 사항.
  (참고로 `apps/user/next-env.d.ts` 도 같은 목록에 잡히는데 이건 Next 생성 파일이고 이전부터 그랬다.)
- `pnpm-lock.yaml` 이 바뀌었다. workspace 의존성 추가의 기계적 결과이고 정책 파일 수정은 아니다.

**주의사항**

- SEED 스니펫이 `process.env.NODE_ENV` 를 참조해서 `tsconfig.app.json` 의
  `types` 에 `"node"` 를 넣어야 했다. 브라우저 앱에 node 타입이 들어간 상태다.
- `seed-design/ui/*.tsx` 를 **수정하면 안 된다.** CLI 가 `add` 할 때 덮어쓴다.
  동작을 바꿔야 하면 `src/components/` 에 감싸는 컴포넌트를 만든다.
- SEED CLI 는 기본으로 익명 사용 데이터를 수집한다. `seed-design.json` 에서 껐다.
- 폼 요소가 터치 기준이라 데스크톱에서 크다. 인풋 약 56px, 선택 박스 약 80px.
  목록과 필터를 한 화면에 얹으면 세로 공간이 빠르게 부족해진다.
- 저장 버튼에 당근 브랜드 오렌지가 그대로 나온다.

**결정 경위**
처음엔 SEED 스킬 문서만 보고 도입 방법을 확인하려 했는데, 컴포넌트 목록을 열어보니
`pull-to-refresh`, `wheel-picker`, `swipeable-menu-sheet`, `manner-temp`(당근 매너온도)까지
있는 모바일 앱용 시스템이었고 Table 이 없었다. 그래서 "도입한다" 가 아니라 "재본다" 로 범위를 줄였다.

`ui-primitive` 스킬은 직전에 자체 `src/ui/` + `tokens.css` 전제로 썼는데, SEED 도입과 정면으로
어긋나서 아직 커밋 전인 김에 갈아엎었다. 지금은 SEED 에 있으면 CLI 로 가져오고 없을 때만
직접 만드는 순서다. 자체 `tokens.css` 는 만들지 않고 SEED 의 CSS 변수를 쓴다.

strict TS 충돌을 사전에 걱정했는데 `erasableSyntaxOnly`·`verbatimModuleSyntax`·`noUnusedLocals`
셋 다 걸리지 않았다. 대신 예상 못 한 `process` 미정의가 걸렸다.

## 2026-09-01 · e5ce582

**커밋** `docs: 브랜치•배포 정책 추가`

**변동사항**

- 루트 `CLAUDE.md` 에 `## 브랜치`, `## 배포` 섹션 추가 (+43줄)
- 브랜치 네이밍을 `<타입>/<범위>-<설명>` 으로 규정
- PR 흐름과 Vercel 프로젝트 구성 명문화
- PR #1 로 `main` 에 머지

**결정 근거**
브랜치 이름만 보고 어느 앱의 무슨 작업인지 알 수 있어야 해서 범위 접두어(`admin`/`user`/`shared`)를 넣었다.
`dev` 를 스테이징으로 두는 건 축제 당일에 트래픽이 몰리는 프로젝트라 검증 안 된 게 `main` 에 올라가면 안 되기 때문.

**차후 작업 계획**

- `dev` 브랜치 생성 — 현재 보류 중
- admin 디자인 시스템: `styles/tokens.css` 와 `ui/` 프리미티브
- Vite 템플릿 잔해 정리 (`App.css` 182줄, `App.tsx` 카운터)
- Django API 스펙 확정 후 API 클라이언트 계층

**user 전달사항**

- Vercel 프로젝트를 앱별로 2개 분리하기로 확정. Root Directory 는 각각 `apps/user`, `apps/admin`.
- 릴리즈는 `dev` → `main` 머지 시점. `hotfix` 만 `main` 직행.

**주의사항**

- `hotfix` 가 `main` 으로 직행하는데 `dev` 백머지 규칙이 없다. 다음 릴리즈에 `dev` 를 머지하면 핫픽스가 덮인다. 팀 결정 필요.
- admin 은 Vite SPA 라 Vercel rewrite 설정이 없으면 `/booths` 같은 경로로 직접 진입하거나 새로고침할 때 404 가 난다. 라우터 붙일 때 같이 처리할 것.
- `pnpm` 은 corepack 경유로 설치돼 있다. nvm 으로 node 버전을 바꾸면 `corepack enable pnpm` 을 다시 쳐야 한다.

**결정 경위**
"레포별로 폴더 구조를 오버라이드해야 한다" 는 팀 얘기에서 출발했다. 확인해보니 하위 `CLAUDE.md` 는 루트를 덮어쓰는 게 아니라 위에 얹히는 구조였다.
그래서 프레임워크가 강제하는 레이아웃 차이(Next 는 `app/`, Vite 는 자유, Django 는 관례)를 인정하고 공통 표준을 강요하지 말자는 쪽으로 정리했다.

디자인 시스템 논의에서는 `src/ui/` 가 축제 도메인을 몰라야 한다는 경계선을 정했다.
`BoothTable` 은 `components/` 로 가고, `ui/Table` 은 컬럼을 props 로 받는 범용이다.
이 규칙은 `src/ui/` 를 실제로 만들 때 `apps/admin/CLAUDE.md` 에 추가하기로 했다. 아직 폴더가 없어서 규칙이 실제를 못 가리키기 때문.

푸시가 403 으로 막혔던 건 토큰 문제가 아니라 저장소 권한(`push: false`)이었다. 권한을 받아 해결.
